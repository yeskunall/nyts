import test from 'ava';

import Nyts from '../../lib/nyts';

const n = new Nyts(null, null, false);

// This endpoint has never worked...
test.skip(async t => {
  // Add a test when this enpoint is working
});
